version: "2"

linters:
  default: standard # errcheck, govet, ineffassign, staticcheck, unused
  enable:
    - contextcheck
    - copyloopvar
    - dupword
    - durationcheck
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - forcetypeassert
    - gocritic
    - godot
    - gosec
    - intrange
    - misspell
    - nilerr
    - noctx
    - nolintlint
    - predeclared
    - reassign
    - revive
    - sloglint
    - thelper
    - unconvert
    - unparam
    - usestdlibvars
    - usetesting
    - wastedassign
    - whitespace
  settings:
    govet:
      enable-all: true
      disable:
        - fieldalignment # only reports GC pointer-scan ordering here, no size savings
    nolintlint:
      require-explanation: true
      require-specific: true
    sloglint:
      kv-only: true
      key-naming-case: snake
      static-msg: true

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
